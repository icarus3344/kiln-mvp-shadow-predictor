# Luna 实施交接单

## 任务目标

在现有离线影子 MVP 基础上，提高模型可靠性并准备本地服务化。不得连接 DCS，不得生成可直接执行的设备控制量。

## 当前入口

- 配置：`config.json`
- 主程序：`src/kiln_mvp.py`
- 运行说明：`README.md`
- 当前结果：`artifacts/metrics.json`
- 页面：`artifacts/report.html`

## 已验证事实

- 原始 Parquet 可以完整读取并汇总为 243,360 条一分钟记录。
- 稳定工况约 202,097 分钟，占 83.0%。
- 出磨生料成分变化事件约 2,945 次；当前轮廓系数选择 3 类，但聚类质量较弱。
- 游离钙仅有约 1,710 个变化事件，应按事件建模，不能按分钟记录建模。
- 窑况标签只覆盖数据末段，差工况比例低。

## 当前基线结果

- 二次风温：测试 MAE 约 9.29℃，持续值基线约 9.55℃，仅小幅改善。
- 二次风温趋势：Macro-F1 约 0.48。
- 窑况三分类：Macro-F1 约 0.36，差工况召回率约 0.16，不可用。
- 游离钙：MAE 约 0.17，差于持续值基线约 0.12，不可用。
- 原料聚类：3类轮廓系数约 0.15，只能作为探索性特征。

## 实施优先级

### P0：先修评估和标签

1. 增加按周滚动时间验证，保存每折指标，而不是只保留一次 70/15/15 切分。
2. 二次风温同时比较当前值、10分钟均值、ARX、Ridge、LightGBM/CatBoost。
3. 窑况标签改为未来 25–35 分钟窗口规则，并分别测试“窗口最差状态”和“中心时刻状态”。
4. 检查窑况等级是否由二次风温/窑电流规则直接生成；若是，报告为规则复现任务，不包装为独立生产结果。
5. 游离钙分别测试 30/60/90/120 分钟物料延迟，并以化验事件为唯一切分单位。

### P1：提升模型

1. 加入 LightGBM 或 CatBoost，保留线性模型作为基线。
2. 为差工况使用类别权重，并报告 PR-AUC、概率校准、每日误报次数。
3. 游离钙加入上一至三次化验值、原料聚类概率、各时间窗过程统计。
4. 聚类比较 K-Means、GMM，并做按月重采样稳定性检查。
5. 输出按月份、原料类、稳定/过渡工况分组的误差。

### P2：再做部署

1. 将预处理、训练、批量推理拆成独立命令。
2. 增加模型清单：训练数据时间范围、字段、配置、指标和文件哈希。
3. 增加本地只读 API：`/health`、`/predict`、`/model-card`。
4. 页面展示预测、置信度、数据超域状态和安全说明。
5. 输入字段缺失、超范围或模型超域时拒绝推荐。

## 暂定晋级门槛

- 二次风温 MAE 在每个滚动测试折中都优于持续值基线，平均改善至少 10%。
- 趋势 Macro-F1 至少 0.60，并且上升、下降两类召回率均可接受。
- 差工况召回率至少 0.70，同时报告每日误报次数；未达到时不得进入现场影子预警。
- 游离钙必须在各时间折中稳定优于上一化验值基线；否则停止反向优化游离钙。
- 任一模型不得使用 RTO 推荐、未来窗口统计或由目标直接生成的日志/趋势字段作为输入。

## 反向优化边界

预测模型通过门槛后，先建立操纵量动态响应模型，再输出方向建议。设计变量限定为窑喂料、分解炉煤、窑头煤、窑速、高温风机、排风机和篦冷机速度；必须加入历史支持范围、变化速率和设备上下限。第一版只允许“增加/保持/减少”，不得直接给出 DCS 设定值。

## 给 Luna 的建议提示

请先阅读 `README.md`、`config.json`、`artifacts/metrics.json` 和 `src/kiln_mvp.py`。复跑当前基线，确认结果可复现后，从 P0 开始逐项实施。每次改动都保留时间切分、基线比较和安全边界；不要把训练成功等同于可以上线。

